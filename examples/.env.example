# Credentials for @retconned/kick-js — pick ONE of the options below.
# Copy this file to `.env` (already gitignored) and fill in the values.

# ── Option 1: harvested credential "bag" (easiest) ──────────────────────────
# Open kick.com while logged in, paste examples/harvest-credentials.js into
# the DevTools console, and paste the copied bag JSON here (single line):
KICK_BAG=